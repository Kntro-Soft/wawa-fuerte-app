# CODEOWNERS — file boundaries so that 4 devs working in parallel do not collide.
# It is here to NOTIFY, not to block (do not enable "require review" in a 6-hour sprint).
#
# Owners match the team table in README.md. Keep both in sync.

# Fallback — tech lead
*                           @jhosepmyr

# P1 — On-device inference engine (Gemma + MediaPipe)
/lib/core/inference/        @sharvel-irigoyen
/lib/main.dart              @sharvel-irigoyen
/pubspec.yaml               @sharvel-irigoyen
/ios/                       @sharvel-irigoyen

# P2 — RAG + nutrition calculation
/lib/core/rag/              @jhosepmyr
/lib/core/nutrition/        @jhosepmyr

# P3 — UI, screens, TTS
/lib/features/              @farioraro
/lib/core/theme/            @farioraro

# P4 — Data and persistence
/lib/core/storage/          @Eric396
/assets/data/               @Eric396
